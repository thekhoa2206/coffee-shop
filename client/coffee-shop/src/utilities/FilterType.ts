export enum OrderOfflineFilterType {
  SYNC_STATUS = "syncStatus",
  CREATED_ON = "createdOn",
}
export enum FilterTypeValue {
  STRING = "string",
  NUMBER = "number",
  DATE = "date",
  DATE_MIN_MAX = "dateMinMax",
}
